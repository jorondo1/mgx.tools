% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rcurve.R
\name{quickRareCurve}
\alias{quickRareCurve}
\title{Quick Rarefaction Curves}
\usage{
quickRareCurve(
  x,
  step = 1,
  sample,
  xlab = "Sample Size",
  ylab = "Species",
  label = TRUE,
  col,
  lty,
  tidy = FALSE,
  max.cores = TRUE,
  nCores = 1,
  ...
)
}
\arguments{
\item{x}{A samples-by-taxa count matrix, data frame, or a `phyloseq` object.}

\item{step}{Step size for the rarefaction subsampling sequence (number of
individuals between subsample points). Smaller values give smoother
curves at higher computational cost.}

\item{sample}{Optional. A rarefaction depth at which to draw a reference
line and interpolate species richness for each sample (plot mode only).}

\item{xlab}{Label for the x-axis (plot mode only). Default `"Sample Size"`.}

\item{ylab}{Label for the y-axis (plot mode only). Default `"Species"`.}

\item{label}{Logical. If `TRUE`, label each curve with its sample name via
`vegan::ordilabel()` (plot mode only).}

\item{col}{Line color(s) for the rarefaction curves (plot mode only).
Recycled to the number of samples. Defaults to `par("col")`.}

\item{lty}{Line type(s) for the rarefaction curves (plot mode only).
Recycled to the number of samples. Defaults to `par("lty")`.}

\item{tidy}{Logical. If `TRUE`, return a tidy `tibble` with columns
`Site`, `Sample`, and `Species` instead of producing a plot, matching
`vegan::rarefy(..., tidy = TRUE)` output.}

\item{max.cores}{Logical. If `TRUE`, use all available cores
(`future::availableCores()`) for parallelization. Overrides `nCores`.}

\item{nCores}{Number of cores to use when `max.cores = FALSE`.}

\item{...}{Additional arguments passed to the underlying plotting
functions (`plot()`, `lines()`, `ordilabel()`); ignored when
`tidy = TRUE`.}
}
\description{
Accelerated version of Vegan::rarecurve, original code by Dave Clark https://dave-clark.github.io/post/speeding-up-rarefaction-curves-for-microbial-community-ecology/

Modified to handle matrix as well as phyloseq object, it has more dependencies (furrr/future). 
Optimized using Claude Sonnet 5 Medium

Tidy option has been added back.
}
